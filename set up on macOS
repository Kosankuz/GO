mkdir -p ~/Documents/Go
mkdir -p ~/Documents/Go/src
mkdir -p ~/Documents/Go/src/Hello
mkdir -p ~/Documents/Go/pkg
mkdir -p ~/Documents/Go/bin

cd ~

//open .bash_profile

nano .bash_profile

// and paste the below on the bottom of the file

export GOPATH=/Users/konstantinkuzmenko/Documents/Go

export GOBIN=$GOPATH/bin

export PATH=$PATH:$GOPATH:$GOBIN

export PATH


Now restart terminal (you can do it by holding two keys together : cmd+Q on a keyboard)

to check 

echo $GOPATH
echo $GOBIN
cat .bash_profile
